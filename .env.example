# Optional: Backend API URL (defaults to http://localhost:3001/api when unset)
# On Vercel, set this to your deployed API URL if you use the Nest API.
# NEXT_PUBLIC_API_URL=https://your-api.vercel.app/api
